export interface ProductType {
	name: string;
	price: number;
	quantity: number;
}

export interface CashType {
	amount: number;
}

export interface UserProfileType {
	id: string;
	name: string;
    balance: number

}
